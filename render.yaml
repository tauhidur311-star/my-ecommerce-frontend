services:
  - type: web
    name: my-ecommerce-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run serve
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000